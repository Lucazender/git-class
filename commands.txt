

 ;>command for listing configuring details

git congig --list

>command for adding username

git config --global user.name "username"

>command for adding user eamil

git config -- global user.email "email"

>command for initializing git repository

git init

>command for listing all untracked files

git status

>command for for adding all untracked files to staging area

git add -A

>command for for adding all files to local repository

git commit -m "commit message"

>command for adding remote repository

git remote add origin remote_path

>command for listing remote

git remote -v

>command for pushing

git push -u name branchname

>command for listing all branches

git branch

>commit history

git log

Branch name: master, main


Create repository, give a name, copy path, check status in terminal, add repository in terminal, verify the process, check status, if not added - add and commit, push to remote